<h1>Editing Book</h1>
<%= form_with model: @list do |f| %>

<div class="field">本のタイトル</div>
<%= f.text_field :title %>

<div class="field">感想</div>
<%= f.text_area :body %>

<%= f.submit 'Update Book' %>
<% end %>

<%= link_to "Edit", edit_book_path(@book.id) %>
<%= "|" %>
<a href="books">Back</a>